我将执行以下修改来解决您的问题：

### 1. 解决“当前委托”无数据问题

* **后端优化** (`backend/okx_client.py` & `main.py`):

  * 修改 `get_orders_pending` 接口，使其支持 `instType` (产品类型) 参数。

  * 在 API 层面默认请求 **SWAP (永续合约)** 的挂单数据（此前可能因为未指定类型或类型不匹配导致数据为空）。

* **前端调整 (`frontend/src/stores/market.js`)**:

  * 请求“当前委托”时明确指定 `instType=SWAP`，确保能拉取到您的合约挂单。

  * *注：如果您的挂单是止盈止损单（策略单），OKX 归类为“策略委托”，普通委托接口无法获取。本次修复针对普通限价挂单。*

### 2. 优化“当前持仓”显示

* **前端组件 (`frontend/src/components/AccountBottomPanel.vue`)**:

  * **引入图标逻辑**: 复用 WatchList 中的图标获取策略（优先 OKX 官方源）。

  * **名称精简**: 将 `BTC-USDT-SWAP` 等冗长名称格式化为精简的 `BTC`。

  * **列样式调整**: 在“币种”列中增加图标显示，使其与左侧列表风格保持一致。

### 待确认

* 确认执行以上代码修改。

