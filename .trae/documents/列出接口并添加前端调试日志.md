是的，您说得很对。OKX 官方文档中获取普通挂单的接口确实是 `GET /api/v5/trade/orders-pending`。

### 接口调用链路说明

我们的系统采用了 **前端 -> 本地后端 -> OKX 官方** 的架构，具体的调用关系如下：

1.  **前端 (Vue)** 调用本地后端接口:
    *   `GET /api/orders/pending`

2.  **本地后端 (FastAPI)** 接收请求后，实际上**并行调用了两个 OKX 官方接口**：
    *   **接口 A (您提到的)**: `GET /api/v5/trade/orders-pending` (获取普通限价/市价挂单)
    *   **接口 B (补充)**: `GET /api/v5/trade/orders-algo-pending` (获取策略委托，如未触发的止盈止损单)

3.  **数据合并**: 后端将 A 和 B 的数据合并后返回给前端。

### 为什么之前可能没数据？

之前前端只请求了 `instType=SWAP` (仅永续合约)，如果您有现货挂单或者未触发的策略单，可能就被过滤掉了。现在我已经去掉了这个限制。

### 执行计划

为了让您看得更清楚，我将执行以下操作：

1.  **前端增加日志**: 在 `frontend/src/stores/market.js` 中打印接口返回的原始数据。
    *   这样您在浏览器控制台 (F12) 就能直接看到后端到底返回了什么，是空数组 `[]` 还是有数据但显示有问题。

2.  **后端不做修改**: 确认后端 `okx_client.py` 中的 `get_orders_pending` 方法确实是调用的 `api/v5/trade/orders-pending`，代码逻辑无误。

